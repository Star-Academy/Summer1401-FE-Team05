import {GameGenrePipe} from './game-genre.pipe';

describe('GameGenrePipe', () => {
    it('create an instance', () => {
        const pipe = new GameGenrePipe();
        expect(pipe).toBeTruthy();
    });
});
